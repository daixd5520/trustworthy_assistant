# 智能体说明

## 默认智能体

默认智能体会处理所有消息，除非路由绑定将流量导向特定智能体。

## 多智能体设置

可以同时运行多个智能体：
- 每个智能体都有自己的工作区、会话和记忆
- 路由绑定决定每条消息由哪个智能体处理
- 各个智能体彼此隔离：不能读取对方的会话

## 智能体间通信

智能体之间不会直接通信。
协调通过以下方式发生：
1. 共享工作区文件（如果已配置）
2. 路由层（绑定可以在运行时更新）
3. 人类操作员（通过 gateway API 或 CLI）
